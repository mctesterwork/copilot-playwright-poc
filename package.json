{
  "name": "copilot-playwright-poc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "codegen": "curl -o api-swagger.json https://api.weather.gov/openapi.json && kubb generate && rm -rf src/gen/schemas && npx prettier --write src/gen",
    "test": "playwright test",
    "test:api": "playwright test tests/api",
    "test:api:smoke": "playwright test tests/api --grep @smoke",
    "test:api:regression": "playwright test tests/api --grep @regression",
    "perf:alerts": "artillery run tests/performance/alerts-load.yml",
    "perf:zones": "artillery run tests/performance/zones-load.yml",
    "perf:sigmets": "artillery run tests/performance/sigmets-load.yml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^10.1.0",
    "@kubb/cli": "^4.1.3",
    "@kubb/core": "^4.1.3",
    "@kubb/plugin-ts": "^4.1.3",
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.8.1",
    "@types/react": "^18.2.21",
    "@types/scheduler": "^0.26.0",
    "prettier": "^3.6.2",
    "scheduler": "^0.27.0"
  }
}
